'use strict';

var ah = ['kuty', 'macsk', 'cic']
// add to all elements an 'a' on the end
for (var i = 0; i < ah.length; i++) {
  ah[i] += 'a';
}
console.log(ah);

while (i < ah.length) {
   ag[i] += 'a';
   i++;
}
console.log(ah)
